import { CnChartsDirectiveDirective } from './cn-charts-directive.directive';

describe('CnChartsDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CnChartsDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
