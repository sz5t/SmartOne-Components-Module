import { CnComponentDirective } from './cn-component.directive';

describe('CnComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new CnComponentDirective();
    expect(directive).toBeTruthy();
  });
});
