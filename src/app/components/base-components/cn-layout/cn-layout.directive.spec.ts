import { CnLayoutDirective } from './cn-layout.directive';

describe('CnLayoutDirective', () => {
  it('should create an instance', () => {
    const directive = new CnLayoutDirective();
    expect(directive).toBeTruthy();
  });
});
